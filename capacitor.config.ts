import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mysticalrabbitbloommain',
  appName: 'mystical-rabbit-bloom-main',
  webDir: 'dist'
};

export default config;
